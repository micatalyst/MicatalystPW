import '@/styles/pages/projects.scss'

import Link from 'next/link'

export default async function Projects() {
  /* const areas = [
    { href: '/projects/webdev', title: 'Web Development' },
    { href: '/projects/photography', title: 'Photography' },
    { href: '/projects/videography', title: 'Videography' },
    { href: '/projects/design', title: 'Graphic Design' },
  ] */

  return <main className="main projects">
    
  </main>
}

{
  /* <div className="projects-list">
        {areas.map((area, index) => (
          <Link key={index} href={area.href} className="project-card">
            <div className="project-card-content"></div>
            <div className="project-card-title">{area.title}</div>
          </Link>
        ))}
      </div> */
}
